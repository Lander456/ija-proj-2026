package common.enums;

/**
 * Defines an enum with all different unit types present in the game.
 * @author Tadeas Topinka (xtopint00)
 */
public enum UnitTypes {
    Tank,
    Infantry,
    Artillery
}
