package common.enums;

/**
 * Defines an enum with the two different possible game states PAUSED (replaying), or PLAY.
 * @author Tadeas Topinka (xtopint00)
 */
public enum GameState {
    PLAY,
    PAUSE
}
