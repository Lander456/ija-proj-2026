package common.enums;

/**
 * Defines and enum containing all different movement types present in the game, on foot (FEET) and wheeled (WHEELS).
 * @author Tadeas Topinka (xtopint00)
 */
public enum MovementTypes {
    FEET,
    WHEELS
}
